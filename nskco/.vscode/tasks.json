{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "(gdb) Launch nskco",
            "type": "shell",
            "command": "cd ${workspaceFolder}/build && rm -rf CMakeFiles && cmake .. && make",
            "group": "build"
        },
        {
            "label": "test",
            "type": "shell",
            "command": "mvn -B test",
            "group": "test"
        }
    ]
}